# 贡献指南

## 新增工具

1. 在项目根目录新增 HTML 页面，或后续迁移到统一页面目录。
2. 在 `src/data/tools.js` 注册工具。
3. 执行 `npm run check`。

## 规则

- 重构时必须保持旧页面可运行。
- Pull Request 尽量小，一次只改一个边界。
- 禁止提交密钥、密码、书签、笔记、LLM 配置等私人数据。
- 新公共脚本放到 `src/js`。
- 新公共样式放到 `src/css`。
- 本地导出的 `data/` 不进入仓库。
